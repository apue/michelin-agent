# Deprecated Logic

Status: accepted

All pre-refactor runtime modules and profiling scripts are removal candidates. They are replaced only after the generic package passes contract, regression, CLI, build, Skill, and hygiene validation. No compatibility shim is retained because the repository has no published API or tracked consumer.
